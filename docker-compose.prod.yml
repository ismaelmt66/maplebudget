services:
  api:
    environment:
      DEBUG: "false"
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
    restart: always

  web:
    restart: always

  db:
    restart: always
